{
  "apiType": "stageMode",
  "buildOption": {
    "arkOptions": {
      // API keys are injected at build time via BuildProfile.
      // Run `python setup-env.py` from the project root to generate the real
      // build-profile.json5 from your local .env file.
      "buildProfileFields": {
        "OPENAI_API_KEY": "",
        "GOOGLE_PLACES_API_KEY": ""
      }
    },
    "resOptions": {
      "copyCodeResource": {
        "enable": false
      }
    }
  },
  "buildOptionSet": [
    {
      "name": "release",
      "arkOptions": {
        "obfuscation": {
          "ruleOptions": {
            "enable": false,
            "files": [
              "./obfuscation-rules.txt"
            ]
          }
        }
      }
    },
  ],
  "targets": [
    {
      "name": "default"
    },
    {
      "name": "ohosTest",
    }
  ]
}
